import React from 'react';

const News = () => {
    // return (
    //     <div className={style}>
    //         {newsData.map((item) => (
    //             <NewsCart key={item.id} item={item} />
    //         ))}
    //     </div>
    // );
};

export default News;